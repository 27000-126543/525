<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.platform.iam.mapper.SysRoleMapper">

    <resultMap id="roleResultMap" type="com.ecommerce.platform.iam.entity.SysRole">
        <id column="id" property="id"/>
        <result column="role_code" property="roleCode"/>
        <result column="role_name" property="roleName"/>
        <result column="data_scope" property="dataScope"/>
        <result column="status" property="status"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="tenant_id" property="tenantId"/>
    </resultMap>

    <select id="selectRolesByUserId" resultMap="roleResultMap">
        SELECT r.*
        FROM sys_user_role ur
        INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        AND r.deleted = 0
        AND r.status = 1
        ORDER BY r.sort_order
    </select>

    <select id="selectPermsByRoleId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_role_menu rm
        INNER JOIN sys_menu m ON rm.menu_id = m.id
        WHERE rm.role_id = #{roleId}
        AND m.deleted = 0
        AND m.perms IS NOT NULL
        AND m.perms != ''
    </select>
</mapper>
