spring:
  application:
    name: ecommerce-recommendation
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: ecommerce
      config:
        server-addr: localhost:8848
        namespace: ecommerce
        file-extension: yml
